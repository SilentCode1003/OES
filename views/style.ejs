<style>
  .input-button {
    background: linear-gradient(to bottom left, #F0FF00 0%, #ff99cc 100%);
  }

  button {
    /* width: 10em; */
    background-color: #00ffd5;
    padding: 10px;
    text-align: center;
    text-decoration: none;
    display: inline-block;
    font-size: 16px;
    margin: 4px 2px;
    cursor: pointer;
    font-size: medium;
    font-family: Arial, "Helvetica Neue", Helvetica, sans-serif;
    font-weight: 400;
    border-radius: 12px;
    width: 10em;
  }

  .entry-area {
    background: white;
    padding: 3px 3px;
    position: flex;
    padding-left: 3em;
    padding-top: 1em;
    display: flex;
    justify-content: center;
  }

  .entry-input {
    text-align: center;
    width: 80%;
  }

  .entry-input-select {
    text-align: center;
    width: auto;
  }

  .approve-btn {
    /* background-color: #9BD7A2; */
    /* background: linear-gradient(to right, #015701 10%, #003300 90%); */
    background: linear-gradient(to right, #009999 10%, #000000 90%);
    color: white;
  }

  .content-bg {
    /* background: linear-gradient(to right, #015701 10%, #003300 90%); */
    background: linear-gradient(to right, #009999 10%, #000000 90%);
  }

  hr {
    border: 0.2em solid white;
    border-radius: 60px;
  }

  table {
    width: 100%;
    border-collapse: collapse;
    background: linear-gradient(to left, #7c7272 20%, #7c7272 70%);

  }

  table,
  th,
  td {
    border: 2px solid black;
    flex-wrap: wrap;
    /* white-space: nowrap; */
  }

  table.result-table {
    width: 40px;
  }

  table.result-table td:nth-child(1) {
    width: 33%;
    text-align: left;
  }

  table.result-table td:nth-child(2) {
    width: 280px;
    text-align: left;
  }

  table.result-table td:nth-child(3) {
    width: 40px;
    text-align: center;
  }

  table.result-table td:nth-child(4) {
    width: 40px;
    text-align: center;
  }

  table.result-table td:nth-child(5) {
    width: 40px;
    text-align: center;
  }

  table.result-table td:nth-child(6) {
    width: 40px;
    text-align: center;
  }

  table.result-table td:nth-child(7) {
    width: 40px;
    text-align: center;
  }

  table.result-table td:nth-child(8) {
    width: 40px;
    text-align: center;
  }

  /* .question-table tr {
   white-space: nowrap;
  } */

  th {
    background-color: #000000;
    color: white;
    text-align: center;
  }

  /* table.question td{
    width: 100%;
  }

  table .criteria {
    width: 33%;
  } */

  .question-table {
    white-space: nowrap;
  }

  td {
    color: white;
    text-align: left;
  }

  table td.shrink {
    white-space: nowrap
  }

  table td.expand {
    width: 99%
  }

  tr:hover {
    background-color: #2c0701;
  }

  button:hover {
    color: black;
    box-shadow: 0 5px 15px rgb(6, 39, 38);
    background: white;
  }

  .bg-topbar {
    /* background: linear-gradient(to bottom, #003300 80%, #00cc00 100%); */
    background: linear-gradient(to bottom, #000000 80%, #009999 100%);
  }

  .loader-wrapper {
    width: 100%;
    height: 100%;
    position: absolute;
    top: 0;
    left: 0;
    background-color: #669999;
    opacity: 60%;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .loader {
    display: inline-block;
    width: 40px;
    height: 40px;
    position: relative;
    border: 4px solid #Fff;
    animation: loader 2s infinite ease;
  }

  .loader-inner {
    vertical-align: top;
    display: inline-block;
    width: 100%;
    background-color: #fff;
    animation: loader-inner 2s infinite ease-in;
  }

  @keyframes loader {
    0% {
      transform: rotate(0deg);
    }

    25% {
      transform: rotate(180deg);
    }

    50% {
      transform: rotate(180deg);
    }

    75% {
      transform: rotate(360deg);
    }

    100% {
      transform: rotate(360deg);
    }
  }

  @keyframes loader-inner {
    0% {
      height: 0%;
    }

    25% {
      height: 0%;
    }

    50% {
      height: 100%;
    }

    75% {
      height: 100%;
    }

    100% {
      height: 0%;
    }
  }

  .center-content {
    display: flex;
    justify-content: center;
  }

  .center-text {
    text-align: center;
  }

  .logo {
    border-radius: 12px;
    padding: 5px;
    width: 98px;
  }

  /* MODAL STYLING */
  /* The Close Button */
  body {
    font-family: Arial, Helvetica, sans-serif;
  }

  /* The Modal (background) */
  .modal {
    display: none;
    /* Hidden by default */
    position: fixed;
    /* Stay in place */
    z-index: 1;
    /* Sit on top */
    padding-top: 100px;
    /* Location of the box */
    left: 0;
    top: 0;
    width: 100%;
    /* Full width */
    height: 100%;
    /* Full height */
    overflow: auto;
    /* Enable scroll if needed */
    background-color: rgb(255, 253, 253);
    /* Fallback color */
    background-color: rgba(255, 255, 255, 0.4);
    /* Black w/ opacity */
  }

  /* Modal Content */
  .modal-content {
    position: relative;
    background: white;
    margin: auto;
    padding: 0;
    border: 1px solid #888;
    width: 80%;
    box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2), 0 6px 20px 0 rgba(0, 0, 0, 0.19);
    -webkit-animation-name: animatetop;
    -webkit-animation-duration: 0.4s;
    animation-name: animatetop;
    animation-duration: 0.4s
  }

  #requestModal .modal-content {
    position: relative;
    background: linear-gradient(to top, #000000 30%, #ff0000 100%);
    margin: auto;
    padding: 0;
    border: 1px solid #888;
    width: 80%;
    box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2), 0 6px 20px 0 rgba(0, 0, 0, 0.19);
    -webkit-animation-name: animatetop;
    -webkit-animation-duration: 0.4s;
    animation-name: animatetop;
    animation-duration: 0.4s
  }

  /* Add Animation */
  @-webkit-keyframes animatetop {
    from {
      top: -300px;
      opacity: 0
    }

    to {
      top: 0;
      opacity: 1
    }
  }

  @keyframes animatetop {
    from {
      top: -300px;
      opacity: 0
    }

    to {
      top: 0;
      opacity: 1
    }
  }

  /* The Close Button */
  .close {
    color: black;
    float: right;
    font-size: 28px;
    font-weight: bold;

  }

  .close:hover,
  .close:focus {
    color: #ff4d4d;
    text-decoration: none;
    cursor: pointer;
  }

  .modal-header {
    padding: 2px 16px;
    height: 8em;
    position: relative;
    background-color: white;
    color: #ff4d4d;
    /* align-content: center; */
  }

  .modal-header p {
    margin: 0;
    background: none;
    position: absolute;
    top: 50%;
    left: 50%;
    margin-right: -50%;
    transform: translate(-50%, -50%)
  }

  .btn-glow-modal:hover {
    color: white;
    box-shadow: 1px 15px 15px #00ffff;
    background: linear-gradient(to bottom, #3399ff 0%, #00ffff 100%);
  }

  .btn-glow-modal {
    color: black;
    background: #ff0000;
    height: auto;
    width: auto;
  }

  #modal-button-container button {
    align-items: center;
    border-radius: 4px;
    font-size: 24px;
    font-weight: bold;
    font-family: Cambria, Cochin, Georgia, Times, 'Times New Roman', serif;
    font-style: normal;
  }

  .modal-button-container {
    background: white;
    color: black;
  }

  .modal-footer {
    /* padding: 2px 16px;
  background-color: #5cb85c;
  color: white; */

    height: 3em;
    position: relative;
    background-color: white;
    color: #ff4d4d;
  }

  .modal-buttons {
    height: 3em;
    position: relative;
    margin-left: auto;
    margin-right: auto;
    background-color: white;
    color: #ff4d4d;
  }

  .modal-body {
    color: black;
    /* background: linear-gradient(to top left, #000000 40%, #ff0000 85%); */
  }

  .center-content {
    display: flex;
    justify-content: center;
  }

  .center-text {
    text-align: center;
  }

  .logo {
    border-radius: 12px;
    padding: 5px;
    width: 98px;
  }

  .modal-print-background {
    background-color: white;
  }

  .modal-company-header {
    color: red;
  }

  .hr-black {
    border: 0.2em solid #009999;
    border-radius: 60px;
  }

  div.settings {
    display: grid;
    grid-template-columns: max-content max-content;
    grid-gap: 5px;
  }

  div.settings label {
    text-align: right;
  }

  div.settings select {
    width: auto;
  }

  div.settings label:after {
    content: ":";
  }

  .instructions {
    width: 90%;
  }

  .instructions p {
    text-align: justify;
    text-transform: uppercase;
  }

  .comment-edit {
    /* background: linear-gradient(to right, #00cc99 1%, #003300 70%); */
    /* background: linear-gradient(to right, #009999 10%, #000000 70%); */
  }

  /* Popup container - can be anything you want */
  .popup {
    position: relative;
    display: inline-block;
    cursor: pointer;
    -webkit-user-select: none;
    -moz-user-select: none;
    -ms-user-select: none;
    user-select: none;
  }

  /* The actual popup */
  .popup .popuptext {
    visibility: hidden;
    width: 160px;
    background-color: #555;
    color: #fff;
    text-align: center;
    border-radius: 6px;
    padding: 8px 0;
    position: absolute;
    z-index: 1;
    bottom: 125%;
    left: 50%;
    margin-left: -80px;
  }

  /* Popup arrow */
  .popup .popuptext::after {
    content: "";
    position: absolute;
    top: 100%;
    left: 50%;
    margin-left: -5px;
    border-width: 5px;
    border-style: solid;
    border-color: #555 transparent transparent transparent;
  }

  /* Toggle this class - hide and show the popup */
  .popup .show {
    visibility: visible;
    -webkit-animation: fadeIn 1s;
    animation: fadeIn 1s;
  }

  /* Add animation (fade in the popup) */
  @-webkit-keyframes fadeIn {
    from {
      opacity: 0;
    }

    to {
      opacity: 1;
    }
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
    }

    to {
      opacity: 1;
    }
  }

  .outer {
    width: 100%;
    /* margin: 0 auto; */
    /* background-color: yellow; */
  }

  .inner {
    margin-left: 105px;
    text-align: left;
    /* background-color: orange; */
  }

  .card-monitor div {
    background-color: #ffffff;
    border-radius: 5px;
    cursor: pointer;
  }

  .card-monitor div:hover {
    animation: pulse 0.5s ease;
  }

  @keyframes pulse {
    0% {
      transform: scale(1);
    }

    50% {
      transform: scale(1.1);
    }

    100% {
      transform: scale(1);
    }
  }
</style>